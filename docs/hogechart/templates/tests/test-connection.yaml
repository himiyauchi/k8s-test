apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hogechart.fullname" . }}-test-connection"
  labels:
    {{- include "hogechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hogechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
